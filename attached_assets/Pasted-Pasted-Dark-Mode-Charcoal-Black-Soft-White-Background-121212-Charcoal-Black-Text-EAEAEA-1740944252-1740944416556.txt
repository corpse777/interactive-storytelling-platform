Pasted--Dark-Mode-Charcoal-Black-Soft-White-Background-121212-Charcoal-Black-Text-EAEAEA--1740944252279.txt
🌑 Dark Mode (Charcoal Black & Soft White)
	•	Background: #121212 (Charcoal Black)
	•	Text: #EAEAEA (Soft White)
	•	Accent: #A6A6A6 (Muted Gray)
	•	Hover Effects: #D4D4D4 (Light Gray)
	•	Borders/Dividers: #1F1F1F (Deep Black-Gray)

🖌️ CSS for Dark Mode:

:root {
    --bg-color-dark: #121212;
    --text-color-dark: #EAEAEA;
    --accent-color-dark: #A6A6A6;
    --hover-color-dark: #D4D4D4;
    --border-color-dark: #1F1F1F;
}

body.dark-mode {
    background-color: var(--bg-color-dark);
    color: var(--text-color-dark);
}

a {
    color: var(--accent-color-dark);
    transition: color 0.3s ease;
}

a:hover {
    color: var(--hover-color-dark);
}

button {
    background: var(--accent-color-dark);
    color: var(--bg-color-dark);
    border: 1px solid var(--border-color-dark);
}

button:hover {
    background: var(--hover-color-dark);
}

🌕 Light Mode (Off-White & Charcoal Gray)
	•	Background: #F7F7F7 (Off-White)
	•	Text: #232323 (Charcoal Gray)
	•	Accent: #737373 (Subtle Gray)
	•	Hover Effects: #525252 (Darker Gray)
	•	Borders/Dividers: #D9D9D9 (Soft Gray)

🖌️ CSS for Light Mode:

:root {
    --bg-color-light: #F7F7F7;
    --text-color-light: #232323;
    --accent-color-light: #737373;
    --hover-color-light: #525252;
    --border-color-light: #D9D9D9;
}

body.light-mode {
    background-color: var(--bg-color-light);
    color: var(--text-color-light);
}

a {
    color: var(--accent-color-light);
}

a:hover {
    color: var(--hover-color-light);
}

button {
    background: var(--accent-color-light);
    color: var(--bg-color-light);
    border: 1px solid var(--border-color-light);
}

button:hover {
    background: var(--hover-color-light);
}

🔄 Theme Toggle JavaScript (For Smooth Switching)

const toggleTheme = document.querySelector("#theme-toggle");
const body = document.body;

toggleTheme.addEventListener("click", () => {
    if (body.classList.contains("dark-mode")) {
        body.classList.remove("dark-mode");
        body.classList.add("light-mode");
        localStorage.setItem("theme", "light-mode");
    } else {
        body.classList.remove("light-mode");
        body.classList.add("dark-mode");
        localStorage.setItem("theme", "dark-mode");
    }
});

// Load user's last theme choice
document.addEventListener("DOMContentLoaded", () => {
    const savedTheme = localStorage.getItem("theme");
    if (savedTheme) {
        body.classList.add(savedTheme);
    } else {
        body.classList.add("dark-mode"); // Default to dark mode
    }
});
