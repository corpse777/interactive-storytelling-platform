import { memo } from "react";

// Placeholder empty component (keeping the file for imports to work)
export const LoadingScreen = memo(() => {
  return null;
});

LoadingScreen.displayName = "LoadingScreen";