// This component has been deprecated since night mode has been removed
// Keeping the file for reference, but the component is no longer used

interface SunMoonToggleProps {
  className?: string;
}

export function SunMoonToggle({ className = "" }: SunMoonToggleProps) {
  // Return null since this component is no longer used
  return null;
}